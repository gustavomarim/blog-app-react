import formatDate from './formatDate';
import objects from './objects';
import string from './string';

export default class _ {
  static readonly obj = objects;
  static readonly fd = formatDate;
  static readonly str = string;
}
